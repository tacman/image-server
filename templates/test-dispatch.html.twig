{% extends 'base.html.twig' %}

{% block body %}

    <h1>
        Test posting to {{ path('app_dispatch_process') }}
    </h1>
    {{ form_start(form) }}
    {{ form_rest(form) }}

    <input type="submit" value="POST">
    {{ form_end(form) }}
    {% for result in results %}
        {{ dump(result) }}
    {% endfor %}

    {{ dump(results) }}
{% endblock %}

{% block stylesheets %}

{% endblock %}

