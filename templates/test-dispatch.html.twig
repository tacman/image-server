{% extends 'layout.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}

    <input type="submit" value="POST">
    {{ form_end(form) }}
    {% for result in results %}
        {{ dump(result) }}
    {% endfor %}

    {{ dump(results) }}
{% endblock %}

{% block stylesheets %}

{% endblock %}

